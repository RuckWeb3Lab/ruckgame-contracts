import { ethers } from "hardhat";

describe('RDogStorage', () => {
  const contractFixture = async () => {
    const [
      owner,
      account1,
      account2
    ] = await ethers.getSigners();
  }
})
